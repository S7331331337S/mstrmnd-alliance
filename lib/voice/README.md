# `lib/voice` (spike)

Provider-agnostic types only. See [`docs/VOICE_SPIKE.md`](../../docs/VOICE_SPIKE.md).

Do not add `OPENAI_API_KEY` or other provider secrets here. Native WebRTC wiring lands in a follow-up behind `expo-dev-client`.
